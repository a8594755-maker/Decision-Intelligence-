# Supabase Configuration
# Get these from your Supabase project dashboard: https://supabase.com/dashboard
VITE_SUPABASE_URL=https://cbxvqqqulwytdblivtoe.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_YcRs0N95Mk6BpTFIq0hoRA_jsW6KlHo

# AI Provider API
# Provider keys are configured in Supabase Edge Function secrets (server-side), not in frontend env.
# Example:
# supabase secrets set GEMINI_API_KEY=your_gemini_api_key
# supabase secrets set DEEPSEEK_API_KEY=your_deepseek_api_key

# Decision-Intelligence LLM routing
# Prompt 1-3 -> Gemini model (3.1 Pro)
VITE_DI_GEMINI_MODEL=gemini-3.1-pro-preview

# Prompt 4-5 + chat -> DeepSeek model (V3.2 via deepseek-chat alias)
VITE_DI_DEEPSEEK_MODEL=deepseek-chat
VITE_DI_DEEPSEEK_BASE_URL=https://api.deepseek.com

# ERP Configuration
# For production: Set to false and provide real ERP API credentials
USE_MOCK_ERP=true
ERP_ENDPOINT=https://your-erp-api.example.com
ERP_API_KEY=your_erp_api_key_here

# ML / Async Run API endpoint (used by forecast + async workflow submit/poll/cancel)
VITE_ML_API_URL=http://127.0.0.1:8000

# Async worker DB connection (server-side)
DI_DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:54322/postgres

# Async execution guardrails / timeouts
DI_MAX_ROWS_PER_SHEET=2000000
DI_MAX_SKUS=5000
DI_SOLVER_MAX_SECONDS=90
DI_BOM_MAX_EDGES=200000
DI_BOM_MAX_DEPTH=12
DI_FORECAST_MAX_SERIES=5000
DI_FORECAST_TIMEOUT_SECONDS=90
DI_BOM_TIMEOUT_SECONDS=90
DI_JOB_MAX_ATTEMPTS=3
DI_WORKER_POLL_SECONDS=2
DI_HEARTBEAT_INTERVAL_SECONDS=5

# Risk-Aware Planning (opt-in global flag)
# Set true to force risk-aware mode globally; per-run via settings.risk_mode='on'
VITE_DI_RISK_AWARE=false
